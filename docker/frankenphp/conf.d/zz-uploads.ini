; Multipart catalogs (sound audio + thumbnail) — aligns with Laravel validation (~25MiB audio + ~5MiB image + overhead).
upload_max_filesize = 64M
post_max_size = 64M
memory_limit = 256M
max_execution_time = 120
max_input_time = 120
